import tw, { styled } from 'twin.macro'

export const Input = styled.input(
  tw`p-2 rounded tracking-wide placeholder:italic placeholder:text-slate-400 w-full border-2 bg-gray-100 font-semibold text-center`
)
export const Label = styled.label(tw`text-slate-600 text-left`)
